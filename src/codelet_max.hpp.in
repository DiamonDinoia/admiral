// Generated by CMake from codelet_max.hpp.in. Do not edit the generated file.
//
// Single source of truth for the compiled codelet catalog. The same explicit
// list drives BOTH the per-N TU generation (src/CodeletCatalog.cmake) and the
// runtime routing predicate, so they can never disagree.
#pragma once

#include <array>
#include <cstddef>

namespace admiral {
namespace detail {

inline constexpr std::size_t CODELET_CATALOG_MAX = @ADM_CODELET_MAX@;
inline constexpr std::array<std::size_t, @ADM_CODELET_COUNT@> CODELET_CATALOG_SIZES = {
    @ADM_CODELET_SIZE_CSV@
};

} // namespace detail
} // namespace admiral
