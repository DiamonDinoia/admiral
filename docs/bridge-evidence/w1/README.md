# W1a/b evidence (2026-07-04). asm_v2_results = knob-phase spill census (BOTH paths instantiated; superseded by merge form: 59 instr / 0 reloads, see commit fba8c20). final_loop1.s = merge-form f64 (4,4) inner loop (mca RThr 10.3). cyc_*.csv = perf-stat total cycles base vs cand (the E1 verdict: equal to ±0.05%). ab_*/bench_* = the tsc ratio runs demonstrating the >±30% cross-binary luck (protocol finding). adj_*/pstat_* = knob-phase f64-65536 duplicate-storage regression (~1.4%, +2.8% L2).
