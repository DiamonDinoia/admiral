// Generated by CMake from config.hpp.in. Do not edit the generated file.
//
// Build-time configuration, generated once so the library and every consumer TU
// agree (a plain -D would risk an ODR mismatch on thread_pool between the
// prebuilt engine and a consumer that missed the define).
#pragma once

// 1: multi-threaded plan execution (std::jthread worker pool, links Threads).
// 0: serial no-op pool, with no <thread> and no Threads dependency.
#define ADM_THREADS @ADM_THREADS_01@

// true: admiral::effort::measure times the model's route candidates at plan
// creation and keeps the measured winner. false: the API accepts the measuring
// efforts and behaves as effort::estimate (fixed at library build for both the
// engine and every consumer, like ADM_THREADS above).
//
// A constant, not a macro, so plan.hpp can branch on it with `if constexpr` and
// carries no preprocessor conditionals. ADM_THREADS above cannot follow. It
// selects an #include (<thread>) and a whole class body, which `if constexpr`
// cannot express. The race costs ~20 KB of a 23 MB library, so compiling it in
// and discarding the branch gives up nothing worth a #if.
namespace admiral::detail {
inline constexpr bool adm_measure = @ADM_MEASURE_01@ != 0;
}
